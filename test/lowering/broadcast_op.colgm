func main() {
    var x = 1.1
    var y_0 = broadcast(x, [1])
    var y_1 = broadcast(x, [2])

    var z_0 = broadcast(y_0, [1, 1])
    var z_1 = broadcast(y_1, [2, 2])

    var foo = [0.1, 0.2, 0.3]
    var bar = broadcast(foo, [1, 3])
}